// Copyright (c) 2026 gpe contributors.
//
// Moved to include/gpe/pool.h, where a client can reach it without adding
// gpe's src/ to its include path -- which is what openFXplayer had to do, and
// then everything else in src/ became reachable too. Kept so that include
// still builds.
#pragma once
#include "gpe/pool.h"
